# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Pure Python -- nothing here is compiled. The scene backend lives in
# isaaccapture.viz.robot, which ships with Televiz on Linux.
#
#     uv pip install "isaaccapture[cloudxr]" --find-links=./install/wheels/
#     uv pip install -e ./examples/robot_viz
#     python -m isaaccapture_examples.robot_viz          # needs a headset; fetches its
#                                                       # scene on the first run
#
# `-e` is supported and is the way to iterate; there is no extension to go stale.

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "isaaccapture-examples-robot-viz"
version = "0.0.0"  # Internal example - not versioned
description = "A robot's digital twin rendered into an Isaac Teleop Televiz XR session"
# Bounds match ISAAC_TELEOP_PYTHON_VERSION_MIN / _MAX_EXCLUSIVE in the root CMakeLists.
requires-python = ">=3.11,<3.14"

dependencies = [
    # Unversioned, and a live hazard in both directions: until isaaccapture is
    # published this does not resolve from an index at all, and once it is, it
    # resolves happily against a release that is not this checkout's viz -- and
    # against one with no robot twin at all. Install the locally built wheel first.
    "isaaccapture",
    # app.py imports numpy directly.
    "numpy",
]

[project.optional-dependencies]
dev = ["pytest", "numpy"]

# `namespaces = true` is load-bearing: isaaccapture_examples is a PEP 420 namespace with
# no __init__.py, shared with every other example. Do not add one to make a discovery
# problem go away -- it would stop a second example sharing the namespace.
[tool.setuptools.packages.find]
where = ["python"]
include = ["isaaccapture_examples*"]
namespaces = true

# No package-data: the scene and its meshes belong to isaaccapture.viz.robot, which fetches
# them into a cache on first use. This example ships nothing but Python.

[tool.pytest.ini_options]
pythonpath = ["python"]
