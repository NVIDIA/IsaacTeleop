include "timestamp.fbs";

namespace core;

// OAK-D frame metadata pushed by the OAK-D camera plugin.
table FrameMetadata {
  // Dual timestamp with device and runtime clock domains.
  timestamp: Timestamp (id: 0);

  sequence_number: int (id: 1);
}

root_type FrameMetadata;
